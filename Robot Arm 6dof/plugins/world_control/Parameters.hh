#ifndef DEF_PARAMETERS
#define DEF_PARAMETERS

#include <iostream>
#include <fstream>
#include <string>
#include <algorithm>
#include <cctype>
#include <math.h>
#include <map>

std::map<std::string, double> loadParam(const std::string& path);

#endif
